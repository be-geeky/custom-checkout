define([
    'uiComponent'

], function (Component) {
    'use strict';

    return Component.extend({
        defaults: {
            template: 'Joshi_Checkout/checkout/shipping/additional-block'
        }
    });
});
